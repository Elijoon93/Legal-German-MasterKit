renderLibrary=function(el){const q=(document.querySelector('#bookSearch')?.value||'').toLowerCase(),books=DATA.books.filter(b=>JSON.stringify(b).toLowerCase().includes(q));el.innerHTML=`<section class="v82-head"><div><span>ACADEMIC LIBRARY</span><h2>کتاب‌ها و منابع دانشگاهی</h2><p>کتاب‌ها بر اساس درس و سطح سازمان‌دهی شده‌اند؛ نسخه و فصل نهایی باید با Moodle و دستور استاد کنترل شود.</p></div></section><div class="panel"><div class="v82-toolbar"><input id="bookSearch" class="input" placeholder="جست‌وجوی کتاب، درس یا سطح" value="${esc(q)}"><span>${books.length} کتاب</span></div><div class="v82-lexicon">${books.map(b=>`<article><header><div><small>${b.cat}</small><h3 class="de">${b.title}</h3></div><em>${b.level}</em></header><p>${b.use}</p><dl><dt>روش استفاده</dt><dd>${b.note}</dd></dl></article>`).join('')}</div></div><div class="panel"><h3>منابع رسمی و پایگاه‌های حقوقی</h3><div class="v82-lexicon">${DATA.resources.map(r=>`<article><header><div><small>${r.kind}</small><h3>${r.title}</h3></div></header><p>${r.use}</p><footer><a class="btn secondary" href="${r.url}" target="_blank" rel="noopener">باز کردن منبع رسمی</a></footer></article>`).join('')}</div></div>`};