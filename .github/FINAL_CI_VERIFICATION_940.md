# Legal German MasterKit 9.4.0 — CI Verification

This branch is an exact verification copy of the consolidated `main` release. The only branch-specific file is this marker, used to trigger pull-request GitHub Actions so the source, Chromium, WebKit, responsive, route, PWA, and public-runtime gates can be observed through the connected GitHub tooling.
